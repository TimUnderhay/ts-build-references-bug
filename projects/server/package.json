{
  "name": "server",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/ && tsc --build -v"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "5.4.5"
  }
}
